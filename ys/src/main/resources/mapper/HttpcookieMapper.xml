<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.yusui.xb.modules.base.dao.mapper.HttpcookieMapper">

    <select id="selectListPage" resultType="cn.yusui.xb.modules.base.entity.Httpcookie">
        select t.*
        from
        t_httpcookie t
        <where>
            1=1

        </where>
    </select>

</mapper>